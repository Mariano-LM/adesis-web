'use strict';const q=s=>document.querySelector(s),nav=q('header .primary-nav'),toggle=q('.toggle'),year=q('#year');if(year)year.textContent=new Date().getFullYear();function closeNav(){if(!nav)return;nav.classList.remove('open');if(toggle)toggle.setAttribute('aria-expanded','false')}if(toggle&&nav){toggle.setAttribute('aria-expanded','false');toggle.onclick=()=>{const o=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(o))};nav.addEventListener('click',e=>{if(e.target.closest('a'))closeNav()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeNav()});addEventListener('resize',()=>{if(innerWidth>760)closeNav()},{passive:true})}const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));const c=q('#particles'),x=c.getContext('2d'),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let p=[],w,h,d=1;function size(){d=Math.min(devicePixelRatio||1,1.6);w=innerWidth;h=innerHeight;c.width=w*d;c.height=h*d;c.style.width=w+'px';c.style.height=h+'px';x.setTransform(d,0,0,d,0,0);const n=Math.min(w<700?55:105,Math.floor(w*h/11500));p=Array.from({length:n},()=>({x:Math.random()*w,y:Math.random()*h,r:.35+Math.random()*1.05,a:.18+Math.random()*.55,vx:(Math.random()-.5)*.025,vy:(Math.random()-.5)*.018,ph:Math.random()*6.28,sp:.002+Math.random()*.006}))}function draw(t=0){x.clearRect(0,0,w,h);for(const s of p){if(!reduce){s.x=(s.x+s.vx+w)%w;s.y=(s.y+s.vy+h)%h}const pulse=reduce?1:.72+.28*Math.sin(t*s.sp+s.ph);x.beginPath();x.arc(s.x,s.y,s.r*pulse,0,6.283);x.fillStyle='rgba(150,205,255,'+(s.a*pulse)+')';x.shadowBlur=s.r>1?9:3;x.shadowColor='#65adff';x.fill()}x.shadowBlur=0;if(!reduce)requestAnimationFrame(draw)}addEventListener('resize',size,{passive:true});size();draw();